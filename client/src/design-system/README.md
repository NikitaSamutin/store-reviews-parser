# Дизайн-система (Design System)

Этот документ описывает основные принципы дизайна и компоненты пользовательского интерфейса проекта Parser Reviews.

## 🎨 Цветовая палитра (Colors)

Проект использует цветовую палитру Tailwind CSS с расширением для основного цвета (`primary`).

### Primary (Blue)
Используется для основных действий, акцентов и активных состояний.
- `primary-50` - `primary-900`: См. `tailwind.config.js`

### Neutral (Gray)
Используется для текста, фона, границ.
- `gray-50` - `gray-900`: См. `tailwind.config.js`

### Dark Mode
Темная тема поддерживается через класс `.dark` на корневом элементе (`<html>`).
- Фон страницы: `bg-gray-50` (Light) / `dark:bg-gray-900` (Dark)
- Фон карточек: `bg-white` (Light) / `dark:bg-gray-800` (Dark)
- Текст основной: `text-gray-900` (Light) / `dark:text-gray-100` (Dark)
- Текст вторичный: `text-gray-500` (Light) / `dark:text-gray-400` (Dark)
- Границы: `border-gray-200` (Light) / `dark:border-gray-700` (Dark)

## 🧩 Компоненты (Components)

Базовые компоненты определены как CSS-классы в `index.css` для удобства использования с Tailwind.

### Кнопки (Buttons)

Базовый класс: `.btn`

Варианты:
- `.btn-primary`: Основное действие (синий фон, белый текст).
- `.btn-secondary`: Вторичное действие (серый фон).
- `.btn-outline`: Контурная кнопка (белый фон, серая рамка).
- `.btn-ghost`: Прозрачная кнопка.

Размеры:
- `.btn-sm`: Маленькая (h-8)
- `.btn-md`: Средняя (h-10) - по умолчанию
- `.btn-lg`: Большая (h-12)

Пример:
```tsx
<button className="btn btn-primary btn-md">Сохранить</button>
<button className="btn btn-outline btn-sm">Отмена</button>
```

### Поля ввода (Inputs)

Базовый класс: `.input`

Автоматически адаптируется под темную тему.

Пример:
```tsx
<input type="text" className="input" placeholder="Введите текст..." />
<select className="input">...</select>
```

### Карточки (Cards)

Используются для группировки контента.

Классы:
- `.card`: Контейнер с рамкой и тенью.
- `.card-header`: Заголовок карточки.
- `.card-title`: Название карточки (h1/h2).
- `.card-description`: Описание под заголовком.
- `.card-content`: Основное содержимое.
- `.card-footer`: Подвал карточки.

Пример:
```tsx
<div className="card">
  <div className="card-header">
    <h2 className="card-title">Заголовок</h2>
    <p className="card-description">Описание</p>
  </div>
  <div className="card-content">
    Контент
  </div>
</div>
```

## 🔤 Типографика (Typography)

Шрифт: `Inter` (sans-serif).

- Заголовки: `font-bold` или `font-semibold`.
- Основной текст: `text-sm` или `text-base`.
- Мелкий текст: `text-xs`.

## 🌙 Принципы Dark Mode

1. **Всегда** используйте префикс `dark:` для цветов, которые должны меняться.
2. Для текста на темном фоне используйте `gray-100` (основной) и `gray-400` (вторичный).
3. Избегайте чисто черного фона (`#000000`), используйте `gray-900` или `gray-800`.
4. Границы в темной теме должны быть тонкими и малозаметными (`gray-700`).
