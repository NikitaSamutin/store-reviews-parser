# Production environment for Netlify deployment
# API is hosted on Railway, so we use absolute URL
# This will be overridden by Netlify environment variable if set
VITE_API_URL=https://store-reviews-api-production.up.railway.app/api
