# Port configuration
PORT=3000

# Environment
NODE_ENV=production

# Database (optional - will use in-memory if not available)
# DATABASE_PATH=/app/data/reviews.db

# CORS origins (comma-separated)
CORS_ORIGINS=https://app-reviews-hub.windsurf.build,http://localhost:3002

# Export directory
EXPORT_DIR=/app/exports
