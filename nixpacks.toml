# Railway Nixpacks конфигурация для API сервиса

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "npm cache clean --force",
    "npm install --prefer-offline --no-audit"
]

[phases.build]
cmds = [
    "npm run build:api"
]

[start]
cmd = "npm start"
